<script>
  var $mensaje1 = $('<span class="  color_acentuado-text"> La clave ingresada es válida</span>');
  $(document).ready(function(){
    $('.tooltipped').tooltip({delay: 50});
  });
  var $mensaje2 = $('<span class="  color_acentuado-text"> La clave ingresada no es válida</span>');


</script>

<div class= "row boxer" ng-controller="RecoverPageCtrl" >
   <div class=" z-depth-4 card-panel col s4 offset-s4 ">
     
       <div class="row ">
      
        <img class="col s6 offset-s3"src="/images/iconobigsinfondo.png" alt="">
       <div class ="row center">
          <h4 class=" light-green-text"> Recuperar clave Visual Mail:</h4>
       </div>
       </div>

    <div ng-show="mostrar">
       <div class= "row ">

         <div class="input-field col s10 offset-s1">
           <i class= "light-green-text small material-icons prefix">
             person_pin</i>
           <input type="text" name="clave" id="clave" ng-model="clave">
           <label for="clave" > Ingrese la clave enviada por correo</label>
         </div>
       </div>

    
       <div class="row  container"> 
          <button type="submit" class="waves-effect waves-light btn-large light-green col s6" ng-click="sendData()" > Verificar clave</button>
          
       </div>
    </div>

    <div ng-show="mostrarpost" class="row container">
      <h6 class="center green-text"> Se ha enviado un correo a la dirección:{{email}}</h6>
        <a href="/" class="waves-effect waves-light btn-large light-green col s6 offset-s3"> Volver</a>
    </div>
    
            <% if(flash && flash.err){ %>
            <script>Materialize.toast($toastContent, 5000);</script>
          <% } %>
    </div>
</div>